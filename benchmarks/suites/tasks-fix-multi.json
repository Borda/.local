[
  {
    "codebase_module": "lightning",
    "difficulty": "medium",
    "expected_files": [
      "early_stopping.py"
    ],
    "expected_patch_keywords": [
      "dry_run",
      "def _run_early_stopping_check",
      "dry_run=False"
    ],
    "id": "FM-01",
    "primary_module": "lightning.pytorch.callbacks.early_stopping",
    "prompt": "In `EarlyStopping` (`lightning/pytorch/callbacks/early_stopping.py`): add a `dry_run: bool = False` parameter to `_run_early_stopping_check`. When `dry_run=True`, log what would happen; the message must interpolate both the computed boolean as `should_stop={should_stop}` and the `reason` value. Do NOT set `trainer.should_stop` or update `self.stopped_epoch` in observe-only mode. Find all callers of `_run_early_stopping_check` in the same file and update their call sites to pass `dry_run=False` explicitly (to preserve current behaviour and document intent). Update the method signature and all call sites.",
    "requires_reset": true,
    "type": "fix_multicaller"
  },
  {
    "codebase_module": "lightning",
    "difficulty": "medium",
    "expected_files": [
      "model_checkpoint.py"
    ],
    "expected_patch_keywords": [
      "reason",
      "def _save_checkpoint",
      "reason="
    ],
    "id": "FM-02",
    "primary_module": "lightning.pytorch.callbacks.model_checkpoint",
    "prompt": "In `ModelCheckpoint` (`lightning/pytorch/callbacks/model_checkpoint.py`): add a `reason: str = ''` parameter to `_save_checkpoint` (defined around line 593). When `reason` is non-empty, call `rank_zero_info` immediately before `trainer.save_checkpoint` with a message that includes both `reason` and `filepath`; preserve the existing save and logger behaviour. Discover and update every production caller of `_save_checkpoint`. Pass the exact reason that matches each save path: `reason='exception'`, `reason='last'`, `reason='none'`, or `reason='top_k'`. Update the method signature and every caller.",
    "requires_reset": true,
    "type": "fix_multicaller"
  },
  {
    "codebase_module": "lightning",
    "difficulty": "hard",
    "expected_files": [
      "strategy.py",
      "ddp.py",
      "fsdp.py",
      "deepspeed.py",
      "model_parallel.py",
      "xla.py"
    ],
    "expected_patch_keywords": [
      "verbose",
      "def setup_environment",
      "super().setup_environment"
    ],
    "id": "FM-03",
    "primary_module": "lightning.pytorch.strategies.strategy",
    "prompt": "In `Strategy.setup_environment` (`lightning/pytorch/strategies/strategy.py`), add a `verbose: bool = False` parameter. When `verbose=True`, log via `log.debug` that strategy-environment setup is beginning before device setup. Discover every production class under `src/lightning/pytorch/strategies/` that overrides `setup_environment`; update each override to add `verbose: bool = False` and forward it through its existing cooperative call as `super().setup_environment(verbose=verbose)`. Preserve every override's existing ordering and strategy-specific behavior. Do not change `Strategy.setup` or add calls to `super().setup`. Treat same-name symbol search as candidate discovery only and verify inheritance and the production package boundary from source before editing.",
    "requires_reset": true,
    "type": "fix_multicaller"
  }
]
